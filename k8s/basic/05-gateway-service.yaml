apiVersion: v1
kind: Service
metadata:
  name: vllm-omni-turbo-gateway-svc
  labels:
    app.kubernetes.io/name: vllm-omni
    app.kubernetes.io/component: inference
spec:
  type: ClusterIP
  selector:
    serving.kserve.io/inferenceservice: vllm-omni-turbo
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8000
