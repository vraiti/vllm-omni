apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: vllm-omni-turbo
spec:
  port:
    targetPort: 8000
  to:
    kind: Service
    name: vllm-omni-turbo-metrics
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
